apiVersion: v1
kind: Service
metadata:
  name: flask-svc
  namespace: flask
spec:
  selector:
    app: flask   # ← This must match pod labels
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000   # match your containerPort

